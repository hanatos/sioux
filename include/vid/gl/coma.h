#pragma once

// TODO: include comanche.h or copy stuff here?
// TODO: provide render routine that knows about offsets
// TODO: draw rotor last, and only one of them

// TODO: do we need vid specific things at all? if no, put this into comanche.h

static inline void
sx_coma_render()
{
  //
    sx_vid_render_geo(obj->geoid[g], omvx, omvq, mvx, mvq);
}
